#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - This is a description
 *
 * Return: 0 always
 */

int main(void)
{
	int a;
	int b;

	for (a = 97; a < 123; a++)
	{
		putchar(a);
	}
	for (b = 65; b < 91; b++)
	{
		putchar(b);
	}
	putchar (012);
	return (0);
}
